# SystemUI Gradle Build System Migration Log

## Current Status
- **Kotlin compilation errors**: 898 (down from ~5000)
- **Resource processing**: PASSING
- **Modules compiling from source**: app, plugin-core, common, utils, log
- **Modules using prebuilt JARs**: shared, unfold, customization, animation, plugin

## Major Milestones

### 1. Custom SDK Platform (SysUISdk)
- Created custom SDK platform at `$ANDROID_HOME/platforms/android-SysUISdk/`
- Contains merged android.jar (SDK + framework.jar) with hidden platform APIs
- Uses `compileSdkPreview = "SysUISdk"` in all Android modules
- 73MB merged jar provides access to all platform internals

### 2. Resource System
- Restored all value resource files from AOSP (strings, colors, dimens, styles, attrs, etc.)
- Fixed duplicate product-specific string resources
- Replaced `androidprv:` (platform-private) with `android:` or local definitions
- Replaced `materialColor*` attrs with Material Components library equivalents
- Added `res-keyguard` resources for bouncer/keyguard
- App namespace set to `com.android.systemui.res` to match AOSP R class imports

### 3. Module Structure
- Simplified from 10 source modules to 5 active modules
- Complex modules (shared, unfold, etc.) use prebuilt JARs from AOSP soong output
- Prebuilt JARs: 44 files in libs/ directory

### 4. Key Technical Decisions
- Use `gradle.projectsEvaluated` to prepend framework.jar to classpath
- Use `kotlin.srcDirs("src")` for AGP 9.x Kotlin file detection
- Use `android.nonTransitiveRClass=false` for transitive resource access
- Use local JARs for unavailable Maven dependencies (lottie, slice)

## Remaining Issues (898 errors)

### Category 1: Settings Proxy (~200 errors)
- `SecureSettings`, `GlobalSettings`, `SystemSettings` - generated by AOSP
- `SettingsProxyExt`, `UserSettingsProxy` - generated
- Functions: `getIntForUser`, `getStringForUser`, `observerFlow`, etc.
- **Solution**: Create Settings proxy stubs or copy from AOSP generated output

### Category 2: Resource References (~100 errors)
- `kg_*` string resources - keyguard strings
- `bouncer_*` dimen resources - bouncer dimensions
- `below_clock_*` dimen resources - clock layout
- `sharedR`, `customR` - cross-module R references
- **Solution**: Add missing string/dimen/style definitions

### Category 3: Flag Stubs (~20 errors)
- `Flags`, `NAMESPACE_SYSTEMUI` - aconfig generated
- **Solution**: Create flag stub classes

### Category 4: Platform Hidden APIs (~50 errors)
- `StatsManager`, `StatsEvent`, `TrafficStateCallback`
- `SafetyCenterManager`, `DeviceConfig`
- `BluetoothLeBroadcast*`, `VcnTransportInfo`
- **Solution**: Already in merged android.jar, may need compileOnly dep

### Category 5: Dagger Generated (~5 errors)
- `DaggerReferenceGlobalRootComponent` - generated by Dagger
- **Solution**: Run Dagger annotation processor

### Category 6: Other (~20 errors)
- `res` extension functions - from SystemUI-common module
- `NeverCompile`, `UsesReflection` - compiler annotations
- Protobuf nano classes

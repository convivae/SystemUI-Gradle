# C5 JarJar E1–E4 有界实验

**日期**：2026-09-01
**状态**：用户已批准精确 Task 079 brief；首次 preflight 因 brief 将“463 个 JAR”误写为“463 个 RSP token”而按合同暂停，chief 已核实实际为 464 token（463 `.jar` + 1 `.srcjar`）并作事实更正
**前置研究**：[`2026-09-01-c5-aconfig-jarjar-closure.md`](./2026-09-01-c5-aconfig-jarjar-closure.md)、[`../architecture/2026-09-01-aosp17-systemui-jarjar-design.md`](../architecture/2026-09-01-aosp17-systemui-jarjar-design.md)

## 背景

Task 078 已把 AOSP 17 platform aconfig JarJar 错位固化为秒级静态 gate，并从 Soong 主源确认：权威 `repackaging.txt` 有 725 条 exact rules（726 物理行），stock SystemUI 的 R8 program input 按产物混用普通和 `repackaged-jarjar` 变体，而 Gradle Release 当前仍保留四个 runtime-critical 原名引用。当前 gate 为：

- Gradle Release：exit 1，30 source（3 definitions）/ 0 target（0 definitions），`RESULT=FAIL`；
- stock SystemUI：exit 0，1 source（1 definition）/ 36 target（0 definitions），`RESULT=PASS`。

Task 078 已否决一次性 `Scope.ALL` JarJar + 删除 hidden definitions 的所有权不安全算法，也否决 post-R8 DEX rewrite 和以 Soong-prebuilt SystemUI code 代替源码编译。pre-R8 变换仍是首选方案族，但具体的 AAR 变体清单、Android/JVM project output 范围以及 R8 对 hidden-name library classes 的真实解析仍缺实验证据，因此不得直接实现。

## 用户裁决

用户已批准执行 E1–E4 有界实验和精确 Task 079 brief。批准只覆盖只读 inventory、scratch 产物变换以及 standalone R8 probe；不覆盖 Gradle 接线、AAR/`libs/` 替换、源码/资源修改或任何 rewrite 实施。

## 实验计划

### E1：464 个 stock R8 输入 token（463 JAR + 1 source JAR）的完整变体与 ownership 清单

逐项解析 `withres/SystemUI.jar.rsp` 的全部 464 个 token（463 个 `.jar` 加 `SystemUI-flag-types.srcjar`），不抽样。每项记录输入路径、Soong owner/variant、是否走 `repackaged-jarjar`、725-rule source/target reference 与 definition 统计、Gradle counterpart 或明确的 no-counterpart 分类、以及是否进入 E2/E3。另对全部 17 个 Gradle 模块分类 Android source / JVM / res-only / source-empty app，并冻结当前 Release project outputs 的路径、SHA-256 与处理策略。

完成门：464/464 已分类（并单列 463 JAR + 1 source JAR）、17/17 模块已分类、无 `UNKNOWN`；任何无法证明的映射使 E1 失败，不得靠猜测补齐。

### E2：受影响 AAR 的 repackaged 变体 scratch 干跑

只在 `/tmp/task079-c5-jarjar-e1-e4/` 中，根据 E1 清单从现有 AOSP `out/` 的 `repackaged-jarjar` 中间产物构造 scratch AAR。不得修改 `libs/` 或 AOSP `out/`。逐个核对：

- 当前 AAR 的非代码 ZIP entries 与 scratch AAR 字节一致；
- class entry name set 一致；
- critical 原名引用归零，所需 hidden 引用出现；
- 725-rule hidden target definitions 始终为零；
- 每个 E1 判定为受影响的 AAR 都有结果，不允许漏项。

### E3：project/JVM 编译产物 scratch JarJar 干跑

使用现有 Release 编译中间产物的只读副本，不启动 Gradle。区分 Android module 的本地 javac/Kotlin outputs 与 external dependencies；不得把 `Scope.ALL` 或合并依赖误当 PROJECT output。JVM modules 单独盘点。

每个拟变换 artifact 在执行前必须满足 725-rule source matching definitions = 0；若不满足，该 artifact 不执行 JarJar，E3 记 FAIL 并将候选算法升级给用户裁决。对满足前置条件的 artifact，以 AOSP host `jarjar.jar` + 冻结规则在 scratch 运行，记录耗时，并断言所有原名 rule references 均按映射消失/转为 target、target definitions = 0。无匹配引用的模块需记录为 no-op，不得悄悄遗漏。

### E4：standalone AGP bundled R8 正/负探针

直接调用：

```text
java -cp <builder-9.3.1.jar> com.android.tools.r8.R8 \
  --release --no-tree-shaking --no-minification --no-desugaring \
  --lib <android.jar> --output <scratch-output> <scratch-program.jar>
```

scratch program 只定义 probe class，以字段/签名引用四个 critical hidden targets，不定义任何 target。正向使用 SysUISdk `android.jar`，要求 exit 0、无 missing-class diagnostic、输出仍引用四个 hidden targets 且 target definitions = 0。负向使用官方 `android-37.0/android.jar`，要求非零退出并对四个 hidden targets 给出 missing-class diagnostics。不得添加 `--pg-conf`、`dontwarn` 或任何 suppress 配置。

## 允许与禁止

允许新增一个 Python 只读实验工具及其单测、一个架构实验报告、本 issue 和 Task 079 brief。所有生成的 AAR/JAR/DEX/log/JSON/CSV 只能位于 `/tmp/task079-c5-jarjar-e1-e4/`，不得提交。

禁止运行 Gradle task、Soong/Ninja、ADB、emulator；禁止写 AOSP checkout/`out/`；禁止修改 `*.gradle*`、`libs/**`、源码、资源、manifest、ProGuard、SDK、现有 checker 语义、ADR、AGENTS 或 CHARTER；禁止实施 rewrite。worker 禁止 `git add -A`/`git add .`，只能显式 add Task 079 Allowed Paths。

## 错误数与预期结果

本任务不以编译错误数为指标，也不运行构建。预期 E1 coverage、E2、E3、E4 正向与负向控制均 PASS；如果 E2/E3 falsify 候选，实验任务应如实输出 FAIL/BLOCKED，而不是修改工程或放宽 gate。Release 静态 gate 应保持红灯，stock 应保持绿灯，以证明零行为变更。

## 待解决问题

1. 哪些 stock R8 repackaged inputs 在本项目有直接 AAR counterpart，完整坐标是什么？
2. 哪些 Android/JVM source module 的现有本地 outputs 实际含 725-rule 原名引用？
3. 所有拟变换 project artifacts 是否都满足“零 matching source definitions”的 ownership 前置条件？
4. SysUISdk hidden targets 是否在真实 R8 9.3.16 解析中成立，且官方 base SDK 阴性对照能证明探针有效？
5. E1–E4 完成后，首选方案族是否足以进入另一个用户裁决的实现 brief？

## 执行记录

- 首次 preflight 在 dispatch base `e87f29d23a76004d8a262367f58c841b456d5492` 按冻结输入门暂停：`shlex.split` 与 `wc -w` 均得 464 token，全部唯一，其中 463 个 `.jar`、1 个 `.srcjar`；唯一 source JAR 为 `out/soong/.intermediates/frameworks/base/packages/SystemUI/shared/SystemUI-flag-types/android_common/SystemUI-flag-types.srcjar`。Task 078 的“463”是 JAR-only 计数，Task 079 brief 错将它写成总 token 数。规则文件仍为 SHA-256 `f79a08d…`、726 物理行。未运行 E1–E4，未改仓库或 scratch。
- Chief 将合同事实更正为“464 token = 463 JAR + 1 source JAR”；这保持“覆盖 RSP 全部输入、不抽样”的已批准范围，不新增行为变更或实现授权。

## 执行基线与 preflight 记录

- **恢复执行基线（resumed execution base）**：`eb9bd9c8eb74685d1fea669d3ae03d678c9d5cdd`（chief 修正 464-token 合同后的提交；初始 dispatch base `e87f29d2` 未产生任何仓库/scratch 变更）。完整 preflight 于本基线重跑通过：RSP 464 token（464 unique = 463 `.jar` + 1 `SystemUI-flag-types.srcjar`）；规则文件 SHA-256 `f79a08d481147a5e6a532ec254e6f075ccb661d844b9ac19db764cd085a6de97`、726 物理行；host `jarjar.jar` 存在（SHA-256 `6a1926ff…`）；R8 自报 `R8 9.3.16 (build 65eb2ed…)`；SysUISdk/base-37.0 android.jar、Release/stock comparator 均在位；scratch 目录尚不存在（干净）。
- **过程偏差记录（只读，无行为影响）**：首次 preflight 及恢复后 preflight 中用于 `shlex.split` 计数的辅助脚本以直接 `python3` 运行，违反了“Python 一律 `uv run`”的项目规则（AGENTS §八 / brief Global constraints）。这些调用全部只读（读 RSP/规则文件并打印计数），未写任何文件，不属于实验本体（E1–E4 驱动器及其单测自始使用 `uv run`）。此偏差记录在案，后续所有 Python 一律 `uv run`。
- **replacement checkpoint（`caa677ea`）**：第二个 `joycode/GLM-5.3` low-thinking session 接管首个 worker 的红测试，补齐并修正 synthetic classfile/AAR fixtures，实现 RSP、classfile constant-pool、E2/E3 ownership/invariant、E4 verdict 与稳定 summary 的可复用分析原语。`uv run pytest tools/tests/test_analyze_aconfig_jarjar_experiments.py tools/tests/test_check_aconfig_jarjar_references.py -q` 实测 `45 passed`（19 个 Task 079 tests + 26 个既有 gate tests）。完整 `run` orchestration 尚未实现，E1–E4 仍未执行；CLI 明确返回非零并打印 `INCOMPLETE`，避免中间态伪报成功。
- **replacement 过程偏差**：该 session 曾把一次探索性 JSON 写到 `/tmp/task079-scan1.json`（合同只允许 `/tmp/task079-c5-jarjar-e1-e4/`）。Chief 立即中止，文件已删除并确认不存在；另一次同类命令在写入前被终止。未修改 AOSP/`out/`、项目 `build/**`、`libs/**` 或 SDK。因该 session 再次陷入长篇设计而未实现 replay，Chief 保留绿色原语 checkpoint 并把余下执行拆为更窄的串行子步骤。
- **第四个 E1 worker 草稿已否决并清理**：该草稿试图把用于 RSP owner 比较的列表由 Gradle module 名改成 AOSP Soong target 名，动机是 RSP intermediate 路径确实暴露 Soong owner；但实现错误地把 Soong owner 本身写成 Gradle counterpart，没有建立显式的 Soong-owner → Gradle-module/category 证据映射。其 `E1=PASS` 还未执行 source/target reference/definition 统计、未冻结 Release local PROJECT output 路径/大小/SHA，并硬编码规则与模块计数，因此是不可接受的假阳性。用户确认后，Chief 已恢复 `tools/analyze_aconfig_jarjar_experiments.py` ���其测试到已推送的 fail-closed checkpoint，删除整个 scratch root，并关闭 worker；该草稿未 commit、未 push。
- **后续执行约束（用户 2026-09-01 明确）**：所有 worker/reviewer 只允许显式 `joycode/Kimi-K3` 或 `joycode/Kimi-K3-jcloud`。Chief 必须持续监控，并在模型/CONTRACT、preflight、首次写入、真实实验启动、pre-commit 与完成等关键步骤给出审查意见；任一 `sleep`/轮询不超过 60 秒。

精确 brief：[`../orchestration/tasks/079-c5-jarjar-e1-e4-experiments.md`](../orchestration/tasks/079-c5-jarjar-e1-e4-experiments.md)。

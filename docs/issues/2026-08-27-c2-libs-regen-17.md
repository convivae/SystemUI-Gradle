# Task 071 — C2: libs/ 全删 + AOSP-17 打包脚本适配 + 脚本再生

## 背景

- AOSP 树 `android-17.0.0_r1` 已全量构建（C1）；C3 源码重对齐完成（task070，对齐门全 0）。
- 本任务执行 ADR 0007 Phase C 验收命题：**git clone 后仅凭 AOSP 树 + tools/ Python 脚本
  可从零复现，无一手工产物**。执行顺序：先改脚本 + pytest 全绿 → sha256 快照 →
  `git rm -r libs/` → 按依赖序脚本再生 → 漂移报告。
- 版本坐标纪律：`install_aar_to_maven.py` 全族 1.x → **2.0.0**（major = AOSP vintage 16→17），
  旧 1.x 目录随全删消失。`libs.versions.toml` catalog 更新归 C4。

## chief 预勘察四项（已逐一实测复核）

1. **iconloader 结构重构**（已复核）：
   - `iconloader/javac` = 3 类（IconFactory、SimpleIconCache 门面）
   - `iconloader_base/javac` = 21 类、`iconloader_base/kotlin` = 120 类
   - 三 jar 两两不相交，闭包 144 类，全部 `com/android/launcher3/`
   - `iconloader/android_common/kotlin/` 在 17 **不存在**（旧冻结路径，已验）
   - res/manifest 源树路径不变；R.txt 改取 `iconloader_base/android_common/R.txt`
2. **WindowManager-Shell-proto 上游删除**（已复核）：
   - Soong 只剩 `WindowManager-Shell-lite-proto`（69 类：
     `desktopmode/data/persistence` 46 + `desktopmode/education/data` 13 + `apptoweb/data` 10）
   - proto/protolog 类并入主 jar：`com/android/wm/shell/ProtoLogController.class`、
     `com/android/internal/protolog/ProtoLogImpl_2006817261($Cache)` 在主 jar 实测存在
   - 脚本删除 nano-proto 条目，主 AAR 吸收
3. **WifiTrackerLib manifest 移位**（已复核）：源树 manifest 已删，改用 soong
   `WifiTrackerLib/android_common/GeneratedManifest.xml`
   （package="com.android.wifitrackerlib.nores"，验存）；res/R.txt 路径不变
4. **motiontoollib 上游删除**（已复核）：`frameworks/libs/systemui/` 无 motiontoollib 目录；
   全 AOSP 无 MotionToolManager；`libs/motion_tool_lib.jar` 不再产出；
   `package_viewcapture_motiontool_jars.py` 只产 view_capture.jar

## 实测新增适配项（预勘察之外，实测 17 树浮出）

5. **view_capture_proto 目标删除**（对应预勘察 "viewcapture 2/4 存活"）：
   17 的 viewcapturelib Soong 只有 `view_capture` 一个 target（bp 无 view_capture_proto）；
   VIEW_INPUTS 缩为 javac(9) + kotlin(13) = 22 类（16 时代为 9+23+24=56 类，proto 类
   随 target 删除而消失）。
6. **SettingsLib DeviceStateRotationLock Kotlin 产物消失**：17 该 target 已 Kotlin→Java
   重写（`PosturesHelper.java` 等），javac jar（13 类）由 discovery 自动发现；
   显式 device-kotlin 输入必须从 code 列表删除（否则 FileNotFoundError）。
7. **SettingsLib 闭包漂移**：34 个 discovery javac jar（884 类，两两不相交）+
   主 Kotlin（488 类）= **1372 类**（16 时代 1153）；Theme Kotlin **29 类**（16 时代 15），
   全部 `com/android/settingslib/widget/`，与主闭包零重叠（实测）。
8. **Traceur 漂移**：TraceurCommon 14 类 + perfetto protos 753 类 = **767 类**（16 时代 640）；
   Traceur res 105 文件不变；FileSender/PresetTraceConfigs/TraceConfig R8 目标存活。
9. **WM-Shell 主产物漂移**：主 javac 1423 + 主 kotlin 1632 + lite-proto 69 = **3124 类**
   （16 时代 1888）；16 时代 exclude 的 3 个 shared AIDL 类在 17 主 jar 已不存在
   （exclude 前缀保留为防御性 no-op）；nano 包全消失；persistence proto 迁入 lite-proto
   命名空间 `desktopmode/data/persistence/`。
10. **SettingsLib per-target res 数漂移**（Batch 4D 十族，实测）：
    MainSwitch 24、AppPreference 91、BannerMessage 112、BarChart 6、Button 29、
    Footer 92、Illustration 9、Slider 4、Usage 2、SettingsSpinner 20，合计 **389**
    （16 时代 346）。
11. **framework-statsd 变体漂移**（再生时浮出）：`framework-statsd.impl` javac 从
    `android_common_apex30` 迁至 `android_common_apex31`；路径修正 + 全 12 条冻结指纹
    按 17 树重新冻结（SystemUI-tags 与 keepanno-annotations 跨 vintage 字节相同）。
12. **aconfig framework family 结构重构**（再生时浮出，重大）：
    - 14 族中 6 族（smartspace/usb/net-platform/permission/controls/devicestate-feature）
      的独立 `android_common/javac` 输出在 17 消失——Soong 将其公开运行时类直接编译进
      **framework-minus-apex 聚合体**，其 javac 以分片产出（`framework.jar0`..`jar60`，
      分片序号非重建稳定，实测各包恰好位于单一分片）。
    - 修正方案：`package_aconfig_jars.py` 新增 `extract_aggregate_subset()`——按包前缀
      **内容扫描发现唯一分片**（要求恰好一个分片含该包、类集恰为五类 runtime 集；
      歧义/缺失/多类即报错），携带真实 Soong 字节进入合并；不固化分片号。
    - permission-flags 独立 javac 仅存于 `android_common_apex36`（APEX 变体）；本任务取
      聚合体的平台变体类（与 16 语义一致）。
    - **security-flags / quickaccesswallet-flags 上游删除**（包改名
      android.security / android.service.quickaccesswallet；旧包全树无踪，
      SystemUI-17 零引用）→ family 14→12。
    - **settingslib-selector-flags 上游删除**（SelectorWithWidgetPreference 不再声明
      aconfig flags lib）→ `libs/settingslib-selector-flags.jar` 不再产出。
    - 合并产物 `systemui-aconfig-flags.jar`：**60 类**（12×5；16 时代 70）。
13. **install_aar_to_maven 坐标表收敛 27→23**：Task 059 直连 AAR 例外四族
    （WifiTrackerLib/iconloader/setupcompat/LowLightDreamLib）从坐标表移除，使脚本默认
    安装形状与 16 时代 libs/maven/ 的 23 族清单一致（ADR 0007 可复现命题）。

## 操作日志

### P0 脚本适配（commit a4b5736a）

- `tools/package_aosp_aar.py`：iconloader 三 jar 闭包 + R.txt 改源；删 WM-Shell
  nano-proto 条目；WifiTrackerLib manifest 改 GeneratedManifest.xml；SettingsLib 删
  device-kotlin 输入
- `tools/package_viewcapture_motiontool_jars.py`：去 motiontool；VIEW_INPUTS 缩为 2 jar
- `tools/install_aar_to_maven.py`：坐标表全族 2.0.0（含 SettingsLib POM 17 条边版本）
- 三个对应测试文件按 17 实测值修正

验证：`uv run pytest tools/tests -q` → **283 passed**（基线 18 failed / 264 passed）。

### P1 快照 + 全删（commit 5a61a52f）

- `find libs -type f | sort | xargs sha256sum > /tmp/task071/pre-delete.sha256`
  → 104 文件 = 28 根 jar + 29 aars + 23 maven AAR + 23 POM + 1 prebuilts jar（与 brief 预期吻合）
- `git rm -r libs/`（104 删除）

### P2 再生（commits 77f5e74e + c02c0dbf）

按 Global Constraints 依赖序执行 7 个脚本（适配缺口 11/12/13 在此阶段浮出并回到脚本
修正后重跑，无跳过无伪造）：

```
uv run python3 tools/package_misc_jars.py --all            # 12 jar 全 MATCH（重冻结后）
uv run python3 tools/package_monet_jar.py                   # monet.jar 72 类（16:56）
uv run python3 tools/package_viewcapture_motiontool_jars.py --all  # view_capture.jar 22 类
uv run python3 tools/package_compilelib_jars.py             # debug/release 各 400 bytes
uv run python3 tools/package_aconfig_jars.py --all          # 12 族合并 60 类 + 11 单独 jar
uv run python3 tools/package_aosp_aar.py --all              # 29 AAR
uv run python3 tools/install_aar_to_maven.py                # 23 AAR + 23 POM（全 2.0.0）
```

全部零报错。再生后 pytest：`uv run pytest tools/tests -q` → **290 passed**。
Gradle 构建：**未运行**（本任务范围排除 gradle）。

### P3 漂移报告

对比基准：删除前 sha256 快照（104 文件）vs 再生后 libs/（102 文件）：

| 类别 | 计数 | 明细 |
|---|---|---|
| byte-identical | 9 | SystemUI-tags.jar、keepanno-annotations.jar、compilelib-debug/release.jar、SettingsLibAdaptiveIcon/AppPreference/BarChart/Color/LayoutPreference.aar（res-only，res 树跨 vintage 未变） |
| drifted | 47 | 23 根 jar + 24 AAR（见下） |
| gone | 48 | 46 maven 1.x 坐标目录 + motion_tool_lib.jar + settingslib-selector-flags.jar |
| new | 46 | 46 maven 2.0.0 坐标目录（同名族升坐标重装） |

漂移 AAR 清单（24）：LowLightDreamLib、SettingsLib、ActionButtons/BannerMessage/Button/
Footer/Illustration/MainSwitch/Progress/RestrictedLock/SelectorWithWidget/SettingsSpinner/
SettingsTheme/Slider/TwoTarget/UsageProgressBar（res 漂移）、Traceur-res、TraceurCommon、
WifiTrackerLib（GeneratedManifest）、WindowManager-Shell-shared、WindowManager-Shell
（主产物+proto 重构）、animationlib、iconloader（三 jar 重构）、setupcompat。

**抽样 5 个漂移 jar 类数对比（16 vs 17，实测）**：

| jar | 16 类数 | 17 类数 | 说明 |
|---|---|---|---|
| framework.jar | 25,914 | 29,066 | turbine-combined 聚合类集膨胀 |
| systemui-flags.jar | 5 | 5 | 五类 runtime 结构不变，字节漂移=新 flag 字段 |
| monet.jar | 56 | 72 | monet+libmonet 类集膨胀 |
| view_capture.jar | 56 | 22 | proto target 上游删除，9+13 两 jar |
| systemui-aconfig-flags.jar | 70 | 60 | family 14→12（两个包上游改名） |

## libs/ 逐文件脚本来源追溯（ADR 0007 验收）

| 产物 | 来源脚本 |
|---|---|
| libs/{framework,framework-statsd,android.car,android_module_lib_stubs_current,SystemUI-proto,SystemUI-statsd,SystemUI-tags,contextualeducationlib,msdl,PlatformMotionTestingComposeValues,keepanno-annotations}.jar（11） | `package_misc_jars.py` |
| libs/prebuilts/tracinglib-platform.jar | `package_misc_jars.py` |
| libs/monet.jar | `package_monet_jar.py` |
| libs/view_capture.jar | `package_viewcapture_motiontool_jars.py` |
| libs/compilelib-{debug,release}.jar | `package_compilelib_jars.py` |
| libs/{systemui-flags,notification-flags,launcher3-flags,settingslib-widget-flags,settingslib-media-flags,device-state-flags,wifi-flags,wm-shell-flags,systemui-shared-flags,settingslib-flags}.jar（10） | `package_aconfig_jars.py` |
| libs/systemui-aconfig-flags.jar | `package_aconfig_jars.py`（12 族合并） |
| libs/aars/*.aar（29） | `package_aosp_aar.py` |
| libs/maven/**（23 AAR + 23 POM） | `install_aar_to_maven.py` |

合计 102 文件，全部脚本产出，无手工拷贝。

## 待解决问题 / C4 移交清单

- `libs.versions.toml` catalog 仍指向已退役 1.x 坐标 → C4 更新为 2.0.0
  （23 族：SettingsLib/SettingsTheme/WindowManager-Shell(-shared)/animationlib/color/
  17 个 per-target 族）。
- 17 新增 aconfig flags 需求（supervision/location/view/telephony 等）→ C4 由编译错误
  驱动逐个补入 `package_aconfig_jars.py`
- `motion_tool_lib.jar` 依赖行 → C4 从 build.gradle.kts 移除
- `settingslib-selector-flags.jar` 依赖行 → C4 从 build.gradle.kts 移除
- view_capture proto 类消失：C4 编译期如出现 `com.android.app.viewcapture.data.*` 引用
  缺失，需回查 17 源码消费方式（当前 SystemUI 17 源码未见引用，预判无影响）
- aconfig family 依赖 `extract_aggregate_subset` 的分片内容扫描：若未来 AOSP 重建后
  分片布局变化导致包跨分片，脚本会报错（设计行为，禁止静默）——届时回查
  framework-minus-apex 构建图
- 架构决策提示（chief 裁量）：17 树不再为 6 族 aconfig 模块产出独立 javac 输出；
  若希望恢复 16 式模块级交付，需在 AOSP 侧显式构建那些 target（本任务选择聚合体
  抽取方案，未改 AOSP 树）
